{
  "routing": {
    "rules": [
      {
        "type": "field",
        "domain": [
          "domain:gstatic.com"
        ],
        "inboundTag": ["tag-vmess"],
        "outboundTag": "direct"
      }
    ]
  }
}
